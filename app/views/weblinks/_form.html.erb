<%= form_for(@weblink) do |f| %>
  <% if @weblink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weblink.errors.count, "error") %> prohibited this weblink from being saved:</h2>

      <ul>
      <% @weblink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
