

<!-- FORM START -->

	  	<%= form_tag '/ideas', :method => 'get' do %>

<% if !@lock_select %>  
	<h2>Listing ideas:</h2>
<% elsif @lock_select %>
	<h2>Filtered ideas:</h2>
<% else %>
	raise "error title"
<% end %>

<div style="display: inline-block;">
	<table style="border: 1px solid black; ">
	  <thead>
	    <tr>
	      <th style="text-align:center">Name</th>
	      <th>Description</th>
	      <th>User</th>

		<% if !@lock_select %>  
		  <th>Worthy?</th>
	    <% elsif @lock_select %>
	    	<td></td>
	    <% else %>
	    	raise "error lock select"
        <% end %>


<!-- REMOVED 1 -->

		  <!-- NOT SHOW WHEN UPLOADED TO HEROKU -->

	      <!--<th>Show</th>
	      <th>Edit</th>
	      <th>Destroy</th>-->

	    </tr>
	  </thead>

	  <tbody>

	  	<% @ideas.each do |idea| %>
	      <tr>
	        <td><%= idea.name %></td>
	        <td><%= idea.description %></td>		

			<!-- USER -->
			      <% if !idea.user.nil? %> 
			        <td><%= idea.user_id %></td>
			      <% elsif %>
			      	<td></td>
				  <% end %>

			<!-- FORM TAGS -->

		<% if !@lock_select %>  
			<td style="text-align:center; vertical-align: top;">
		          <%= label_tag :filter, "" %>
		          <%= check_box_tag 'filter_values[]', idea.id, false %>
				  
		          <%#= check_box(:accepted, 'false', selected:false) %>
	        </td>
	    <% elsif @lock_select %>
	    	<td></td>
	    <% else %>
	    	raise "error lock select"
        <% end %>
        	

<!-- REMOVED 2 -->


			<!-- NOT SHOW WHEN UPLOADED TO HEROKU -->

			<!--
	        <td><%#= link_to 'Show', idea %></td>
	        <td><%#= link_to 'Edit', edit_idea_path(idea) %></td>
	        <td><%#= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	    -->

	      </tr>
	    <% end %>


	<%# if !@lock_select_submit %>  
<!--
		<div style="display: inline-block; float: right;">
			<%#= link_to 'Create Hunch', '/ideas', method: :get %>


			<%#= image_submit_tag("button.png", data: { disable_with: "Please wait.." }) %>
		</div>



	<%# elsif @lock_select_submit %>
		<div>
			<p><%#= link_to 'Show All', '/', method: :get, data: { confirm: 'Are you sure?' } %>
			</p>
		</div>
	<%# else %>
		raise "error lock select"
	<%# end %>
-->


		<% end %> 
<!-- FORM END -->

<!-- FORM 2 START -->
	
	  </tbody>
	</table>

<div class="sorting">

<h2>Choose Action:</h2>
	<% if @lock_select_submit %>
			<p>
				<ul>
					<li>List: <%= link_to 'Show All Ideas', '/', method: :get %> 
				<!-- , data: { confirm: 'Are you sure?' } -->
					</li>
				</ul>
			</p>
	<% end %>
    <% if !@lock_select_submit %>
			<p>
				<ul>
					<li><%= label_tag(:search, "Filter:") %>  
			<%= hidden_field_tag(:idea_name) %>
			<%= submit_tag("Filter Ideas", data: { disable_with: "Please wait.." }) %>
					</li>
				</ul>
			</p>
	<% end %>
			<p>
				<ul>
					<li><%= label_tag(:search, "Search:") %>  
			<%= text_field_tag(:idea_name) %>
			<%= submit_tag("Search Ideas", data: { disable_with: "Please wait.." }) %>
					</li>
				</ul>
			</p>
			<p>
				<ul>
				    <li>Create: <%= link_to 'New Idea', new_idea_path %>
				    </li>
				</ul>
			</p>

		<%= form_tag '/ideas/details', :method => 'post' do %>

	  		<div>

			    <p>
			    	<% @ideas.each do |idea| %>
						<%= hidden_field_tag 'ideas_ids[]', value: idea.id %>
					
				    <% end %>

				    <% if @lock_select_submit %>  
				    	<ul>
				    		<li>Next: <%= submit_tag("Proceed to Detail", data: { disable_with: "Please wait.." }) %>
				    		</li>
				    	</ul>
				    <% end %>
				</p>

			</div>
		<% end %>
</div>

</div>



